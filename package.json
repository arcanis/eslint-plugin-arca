{
  "name": "eslint-plugin-arca",
  "version": "0.4.0",
  "description": "A plugin to make Maël happy",
  "keywords": [
    "eslint",
    "eslintplugin",
    "eslint-plugin"
  ],
  "author": "Maël Nison",
  "main": "lib/index.js",
  "scripts": {
    "test": "$(npm bin)/mocha tests/**/*.js --reporter dot",
    "preversion": "npm test",
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "requireindex": "~1.1.0"
  },
  "devDependencies": {
    "eslint": "~2.5.1",
    "mocha": "^2.4.5"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "license": "ISC"
}
